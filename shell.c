#include "main.h"
/**
 * main - main function
 * @arc: len argument
 * @arv: arv argument
 * Return: 0
int main(int arc, char **arv, char **envp)
{
	arv = arv;
	if (arc == 1)
	{
		prompt(arv, envp);
	}
	return (0);
}
